# ── SERVER ──────────────────────────────────────────
server.port=${PORT:8080}
fastapi.server.url=https://cardiocare-ai.onrender.com

# Kích hoạt profile h2 để chạy offline không cần mạng (bỏ dấu # để kích hoạt)
# spring.profiles.active=h2

# ── DATABASE CONNECTION (SUPABASE) ──────────────────
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.xmegdperrxmprgxqvkaq}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:5RBONt4L9dojNB1G}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── JPA ──────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyHbmImpl

# ── THYMELEAF ────────────────────────────────────────
spring.thymeleaf.cache=${SPRING_THEMELEAF_CACHE:true}
spring.thymeleaf.encoding=UTF-8

# ── AI API ───────────────────────────────────────────
# ai.api.url=http://localhost:8000/predict
ai.api.url=https://cardiocare-ai.onrender.com/predict

# [D.21] Chatbot bác sĩ — endpoint riêng /chat/doctor (KHÁC /chat/patient
# dành cho app bệnh nhân). Cùng FastAPI server, chỉ khác path.
# ai.api.chat.url=http://localhost:8000/chat/doctor
ai.api.chat.url=https://cardiocare-ai.onrender.com/chat/doctor

# ── SESSION ──────────────────────────────────────────
server.servlet.session.timeout=30m

# ── CONNECTION POOL ──────────────────────────────────
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=-1
spring.datasource.hikari.maximum-pool-size=30
spring.jpa.open-in-view=true

# ── ENCODING ─────────────────────────────────────────
spring.mandatory-file-encoding=UTF-8

# ── SEPAY PAYMENT GATEWAY ────────────────────────────
sepay.api.key=OEYETO1H1DYIZMQWCOCINZRB5VXJQQ3KJDR73INFYMFB6VHGTX0MSATIFSOTBUWU
sepay.bank.id=MB
sepay.bank.account=966662869999
sepay.bank.owner=TRAN XUAN THANH

# ── CLINIC FEES ──────────────────────────────────────
clinic.fee.general=150000
clinic.fee.specialist=300000